# DB (如果用 docker-compose: host = mysql)
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=app
DATABASE_PASSWORD=apppassword
DATABASE_NAME=notes_app

# JWT
JWT_SECRET=change_this_to_a_strong_secret
JWT_EXPIRES_IN=7d

# Server
PORT=4000
